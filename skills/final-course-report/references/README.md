# Shared References

This Codex skill uses the repository-level reference files at `../../references`.

Keep detailed reusable guidance there so Codex and other agents can share the same rules through both `skills/final-course-report/SKILL.md` and the root `AGENTS.md`.
